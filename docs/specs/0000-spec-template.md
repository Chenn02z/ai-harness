# Spec: Template

## Status

Draft

## Goal

State the user-visible or developer-visible outcome.

## Scenario

Give one concrete request or workflow that should work when this spec is done.

## Architecture Reference

Link to `docs/ARCHITECTURE.md` and note which approved seams or deferred
entries this spec touches.

## In Scope

- What this spec covers.

## Out Of Scope

- What this spec intentionally does not cover.

## Architecture Seams

Specific boundaries from `docs/ARCHITECTURE.md` this spec must honor. What
must be built to extend cleanly later within those seams — without overbuilding
or adding speculative frameworks.

## Contracts

Define inputs, outputs, agent responsibilities, tool boundaries, schemas, or
document formats that must stay stable.

## Failure Modes

Describe missing data, ambiguous requests, unavailable tools, rejected
assumptions, stale context, or other expected failures.

## Acceptance Criteria

- Observable behavior that proves the spec is satisfied.

## Verification

- Tests, lint, build, manual smoke checks, or review steps required.

## Open Questions

- Decisions that must be resolved before implementation.
